fastapi>=0.110          # web layer (interactive API docs at /docs)
uvicorn>=0.29           # ASGI server — one server on every platform (replaces gunicorn/waitress)
httpx>=0.27             # fastapi.testclient transport (tools/test_app.py, tools/test_security.py)
requests==2.32.3
jsonschema==4.23.0
python-dotenv==1.0.1
PyJWT[crypto]==2.13.0
